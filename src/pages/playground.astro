---
// Minimal route stub so the nav's "Projects" link lands somewhere real.
// The Playground (raw all-projects gallery + photo gallery in the sketch
// aesthetic) is built out in Phase 4.
import Base from '../layouts/Base.astro';
---

<Base title="Playground — Dylan Chen">
  <main class="playground-stub">
    <h1>Playground</h1>
    <p>The sketch-layer — a raw gallery of every project plus the photo
      gallery. Built out in a later phase.</p>
  </main>

  <style>
    .playground-stub {
      padding: clamp(1.5rem, 6vw, 4rem);
      min-height: 60vh;
    }
  </style>
</Base>
